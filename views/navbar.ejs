<nav class="flex items-center min-h-16 shadow-md p-2 bg-primary-dark text-white">
    <div class=" text-xl md:text-3xl font-extrabold grow">Mini Message Board</div>
    <div class="flex align-center justify-center min-h-8 gap-4 text-lg md:gap-8 items-center md:text-xl font-bold">
        <% links.map((link) => { %>
            <a href=<%=link.link %> class="hover:border-b-2 hover:border-black hover:border-solid active:border-b-2 active:border-black active:border-solid">
                <%= link.name %>
            </a>
        <% }) %>
    </div>
</nav>